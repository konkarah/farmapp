# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js build output (CRITICAL: Do not commit)
.next/
out/

# Environment variables (CRITICAL: Keep your secrets safe)
.env
.env.local
.env.*.local

# NOTE: You SHOULD commit your example file as a template for other developers:
# !.env.example
# !.env.local.example

# OS generated files (You are on macOS, so .DS_Store is essential)
.DS_Store
.AppleDouble
.LSOverride

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript cache
*.tsbuildinfo

# Local AI/IDE tool caches (e.g., Qodo/Codium)
.qodo/

# Optional: Deployment configs (Only ignore if ecosystem.config.js contains local machine paths or secrets)
# ecosystem.config.js